# ss是一个要替代netstat的命令，其功能为netstat子集. Linux发行版一般都自带, 其大部分选项的含义和netstat一致
# l 监听, t tcp, u udp, p 程序名
# a 所有, n 不反向解析端口或ip

# 显示所有连接(包括监听和非监听)
ss -a 

# 只显示监听的连接
ss -l 

# 只显示tcp连接
ss -t

# 只显示udp连接
ss -u

# 显示程序名
ss -p
